{
  "name": "html-interactives",
  "version": "1.0.0",
  "private": true,
  "description": "Premium, responsive educational HTML interactives. Each interactive is self-contained static HTML/CSS/JS — no framework, no build step.",
  "scripts": {
    "build": "node tools/build-standalone.js",
    "serve": "node tools/serve.js",
    "test": "playwright test",
    "test:mobile": "playwright test --project=mobile-375",
    "test:tablet": "playwright test --project=tablet-768",
    "test:desktop": "playwright test --project=desktop-1440",
    "test:a11y": "playwright test a11y",
    "test:responsive": "playwright test responsive",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.56.1"
  },
  "comments": {
    "overrides": "@axe-core/playwright depends on a newer playwright-core than @playwright/test, which would expect a browser build that is not installed. Pinning keeps one version across the tree, matching the Chromium already on disk."
  },
  "overrides": {
    "playwright-core": "1.56.1"
  }
}
